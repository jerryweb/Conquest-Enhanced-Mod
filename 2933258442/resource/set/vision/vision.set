{visions
	(define "radius"
		(mod "mp" {radius (* 1.0 %r)})		; // This value changes the global vision ranges of the game.
		(mod not "mp" {radius (* 1.0 %r)})  ; // This value changes the global vision ranges of the game.
	)
	(include "spotting.inc")
	(include "hearing.inc")
	(include "vision_fields.inc")
	(include "vision_fields_ce_ext.inc")
}
