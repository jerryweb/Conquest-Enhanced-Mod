{difficulty
	(include "difficulty_generic.inc")
	{any
		{death_delay_min 600}		; min bleeding out time in seconds
		{death_delay_max 1000}		; max bleeding out time in seconds

		{burst 1.5}					; multiplier to number of bullets in burst from weapon settings
		{health 1.1}				; health multiplier from soldier settings
	}
}
